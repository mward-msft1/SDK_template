[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agentcore-entra-agent-template"
version = "0.1.0"
description = "Beginner Amazon Bedrock AgentCore Runtime template with Entra Agent ID federation"
requires-python = ">=3.10"
dependencies = [
  "bedrock-agentcore>=1.9.1",
  "botocore[crt]>=1.35.0",
  "strands-agents>=1.15.0"
]

[tool.hatch.build.targets.wheel]
packages = ["."]
